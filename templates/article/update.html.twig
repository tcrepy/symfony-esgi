{% extends 'base.html.twig' %}

{% block body %}
    {% include 'back.html.twig' with {'path': 'article_controller_index'} %}

    <div class="row justify-content-md-center"><h1 class="col-sm">Mettre à jour l'article {{ article.name }}</h1></div>
    <div class="row justify-content">
        <div class="col-sm-5">{{ form(form) }}</div>
    </div>
{% endblock %}

{% block title %}
    Mettre à jour l'article {{ article.name }}
{% endblock %}

